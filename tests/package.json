{
  "name": "food-ordering-platform-tests",
  "version": "1.0.0",
  "description": "Cypress tests for FoodExpress Online Food Ordering Platform",
  "scripts": {
    "cy:open": "cypress open --config-file cypress.config.js",
    "cy:run": "cypress run --config-file cypress.config.js",
    "cy:run:chrome": "cypress run --browser chrome --config-file cypress.config.js",
    "cy:run:firefox": "cypress run --browser firefox --config-file cypress.config.js",
    "test": "cypress run --config-file cypress.config.js"
  },
  "devDependencies": {
    "cypress": "^14.5.4"
  },
  "dependencies": {
    "html-pdf": "^3.0.1",
    "markdown-pdf": "^11.0.0",
    "xlsx": "^0.18.5"
  }
}
